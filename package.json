{
  "name": "typescript-boilerplate",
  "description": "a simple template and easy to start for a project using Typescript and Jest",
  "version": "0.0.1",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "license": "MIT",
  "author": {
    "email": "lamhieu.vk@gmail.com",
    "name": "Hieu Lam",
    "url": "https://lamhieu.info"
  },
  "scripts": {
    "clean": "rimraf ./build ./coverage",
    "dev": "tsc -p tsconfig.dev.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "tslib": "^1.10.0"
  }
}
